/*  
Problem: Implement an XOR linked list; it has an add(element) which adds
the element to the end, and a get(index) which returns the node at index.

I'm skipping this because appearently you can't have XOR's in JavaScript
*/
